# QFlow-smart-queue-management-system

QueueLess provides an online system where users can select a service, join its queue, and see their queue number, currently serving number, number of people ahead and estimated waiting time.